using Firebase.Database;

namespace Infrastructure
{
    public class UOW
    {
        private DatabaseReference _database;

        /*
        public UOW(FirebaseDatabase database)
        {
            _database = database;
        }*/

        public void SaveDataToDatabase()
        {
        }
    }
}